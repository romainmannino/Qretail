"use client";import {useEffect,useState} from "react";import {useRouter} from "next/navigation";export default function Scan(){const r=useRouter();const[e,setE]=useState("");useEffect(()=>{let s:any;import("html5-qrcode").then(({Html5QrcodeScanner})=>{s=new Html5QrcodeScanner("reader",{fps:10,qrbox:240},false);s.render((t:string)=>{try{const u=new URL(t);const id=u.searchParams.get("id")||u.pathname.split("/").pop();if(id)r.push("/configure/"+id)}catch{setE("QR code non reconnu")}},()=>{})});return()=>{s?.clear().catch(()=>{})}},[r]);return <main className="wrap"><div className="brand"><b>QR</b>etail</div><div className="card" style={{marginTop:40}}><div className="row"><span className="num">1</span><h2>Scanner une affiche</h2></div><p className="muted">Autorisez l'appareil photo puis placez le QR code dans le cadre.</p><div id="reader"/>{e&&<p>{e}</p>}</div></main>}