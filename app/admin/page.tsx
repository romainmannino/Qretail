"use client";import {useState} from "react";export default function Admin(){const[n,setN]=useState("WILD LT 2027"),[url,setUrl]=useState(""),[qr,setQr]=useState("");async function create(){const r=await fetch("/api/posters",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:n,url})});const d=await r.json();setQr(d.qr)}return <main className="wrap"><div className="top"><div className="brand"><b>QR</b>etail <span className="muted">Admin</span></div><a href="/">Commerçant</a></div><div className="grid"><div className="card"><h2>Créer un QR code dynamique</h2><label>Nom / référence</label><input value={n} onChange={e=>setN(e.target.value)}/><label>URL par défaut</label><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://..."/><button className="btn" onClick={create}>Générer le QR code</button></div>{qr&&<div className="card"><h2>QR prêt</h2><img className="qr" src={qr}/><p><a download="qretail.png" href={qr}>Télécharger le QR</a></p></div>}</div></main>}