"use client";import {useState} from "react";import {useParams} from "next/navigation";export default function Configure(){const{id}=useParams();const[url,setUrl]=useState("");const[msg,setMsg]=useState("");async function save(){const res=await fetch("/api/posters/"+id,{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({url})});setMsg(res.ok?"Affiche configurée avec succès":"Configuration impossible")};return <main className="wrap"><div className="brand"><b>QR</b>etail</div><div className="card" style={{marginTop:40}}><div className="row"><span className="num">2</span><h2>Configurer l'affiche n° {String(id).padStart(3,"0")}</h2></div><label>URL</label><input type="url" placeholder="https://..." value={url} onChange={e=>setUrl(e.target.value)}/><button className="btn" onClick={save}>Enregistrer</button>{msg&&<p>{msg}</p>}</div></main>}